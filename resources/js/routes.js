import VueRouter from "vue-router";

let routes = [
  // {
  //    path: '/',
  //    name:'home',
  //    component: require('../components/advisor-admin-db/Dashboard')
  // }
  
];

export default new VueRouter({
  routes
});
